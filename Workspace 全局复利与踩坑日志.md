 # Workspace 全局复利与踩坑日志

 更新时间：2026-04-08

 ## 已确认有效的长期规则

 1. 顶层 Workspace 只放全局共享规则和所有项目入口。
 2. 单个项目不要再承担全局工作台职责。
 3. 全局文档只保留一份，避免版本混乱。
 4. 新项目初始化后和实际开发要分成两个阶段：
    - 初始化阶段在 Workspace 顶层完成
    - 实际开发阶段则到单独 Project 中完成
 5. 混合工作台要优先放独立项目，知识工作空间、历史资产库，再导入总工作台。

 ## 已踩过的坑

 ### 1. 把单个项目设置成总工作台

 之前把 `Auto_Income_Agent` 当成总入口，结果会导致：

 - 项目和全局规则混在一起
 - 顶层名字不准确
 - 新项目继续被套在项目内部

 ### 2. 把全局文档复制进每个项目

 这样做的问题是：

 - 一个项目更新了，全局规则不等于别的项目也更新了
 - 长期一定混乱
 - 后续 Codex 会误判多个项目不同的"全局规则"

 ### 3. 一直在 Workspace 根目录做项目开发

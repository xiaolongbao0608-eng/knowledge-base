# SCHEMA — 衍谷知识库维护规范（LLM Wiki 模式）

> 本文件是知识库的 **schema**：定义结构、约定与工作流。LLM 据此维护维基；人负责取材、定向与判断。
> 模式来源：用户提供的 `LLM Wiki.md`（F:\codex知识库\LLM Wiki.md）。
> 创建：2026-07-13

---

## 1. 三层架构

| 层 | 目录 | 性质 | LLM 权限 |
|----|------|------|----------|
| **raw（原始资料层）** | `01_公司基础/`~`10_产线工具/` 各主题文件夹内 + 零散导入放 `raw/` | 不可变源文件（PDF/DOCX/XLSX/图片/原始笔记），真理来源 | **只读不写** |
| **wiki（维基层）** | `wiki/` | LLM 撰写的合成页：综述、实体页、概念页、对比页、分析 | **拥有此层**：创建、更新、维护交叉引用 |
| **schema（本文件）** | `SCHEMA.md` | 结构约定与维护纪律 | 人/LLM 共建演进 |

辅助文件：
- `index.md` — 内容目录（按类别罗列每页链接 + 一句话摘要），**每次 ingest 更新**。
- `log.md` — 时间线日志（append-only，条目前缀 `## [YYYY-MM-DD] 动作 | 标题`）。

---

## 2. 目录约定

- `00_导航与规则/` — 导航、规则、本 SCHEMA。
- `01_公司基础/` ~ `10_产线工具/` — **工作树**：按业务主题组织，含源文件与笔记混排，供 Obsidian 浏览与图谱。其中源文件按 raw 层规则视为不可变。
- `wiki/` — 跨主题的 LLM 合成页。
- `raw/` — 不适合归入某主题的零散原始导入。
- `kb-wiki/` — 已实现的**自动化笔记维基**（抖音/Get笔记 ingestion 子系统）：内部 `raw-get笔记/`（原始抓取）+ `wiki/`（合成），遵循同类原则，独立运行。
- `_归档/` — 历史版本（按 知识库规则.md：旧版命名 `原名_YYYYMMDD_版本号`）。

---

## 3. 命名约定

- **源文件**：`[项目]_[内容]_[YYYYMMDD].[ext]`（沿用 `知识库规则.md`）。
- **维基页**：`[类型]_[主题].md`，类型 ∈ {综述, 实体, 概念, 对比, 分析}。交叉引用用 Obsidian `[[]]`。
- **日志条目前缀**：`## [YYYY-MM-DD] ingest|query|lint | 标题`。

---

## 4. 工作流

**Ingest（摄入）**
1. 把新源放入 `raw/` 或对应主题文件夹。
2. 告知 LLM 处理（建议一次一个源、保持参与）。
3. LLM：读源 → 写/更新相关 wiki 页（实体/概念/对比/综述）→ 更新 `index.md` → 在 `log.md` 追加 `## [日期] ingest | 标题`。
4. 单源可能触及 10–15 个 wiki 页；新数据若与旧结论矛盾，显式标注。

**Query（查询）**
1. 向维基提问。
2. LLM 先读 `index.md` 定位，再深入相关页，带引用合成答案。
3. 有价值的答案（对比表、分析、新发现的关联）**回填为新的 wiki 页**，让探索也复利累积。

**Lint（巡检，定期）**
让 LLM 健康巡检：页间矛盾、已被新源推翻的过时结论、无入链的孤儿页、被提及却无独立页的重要概念、缺失交叉引用、可用联网搜索填补的数据缺口。

---

## 5. 人 / LLM 分工

- **人**：取材、定向分析、提问、判断意义。
- **LLM**：摘要、交叉引用、归档、记账——除取材与判断外的一切（维护成本趋近零，故维基得以长期保持）。

---

## 6. 与既有规则的关系

- `00_导航与规则/知识库规则.md` — 面向**人**的使用规则（生产即入库、桌面中转、版本留痕）。
- 本 `SCHEMA.md` — 面向 **LLM** 的维护规范。二者互补。
- `00_导航与规则/索引总表.md` — 人读快速跳转表（legacy）；**LLM 维护的内容目录以 `index.md` 为准**。

## 7. 未来存储规则（硬性）

- 原始源 → 进 `raw/` 或主题文件夹，**不可变**（LLM 只读）。
- LLM 产出（摘要/合成/分析）→ 进 `wiki/`。
- 任何 ingest/query/lint 动作 → 追加 `log.md`。
